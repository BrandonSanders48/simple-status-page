.claude/
.vscode/

# Node / Next.js
node_modules/
.next/
next-env.d.ts
npm-debug.log*
*.tsbuildinfo

# Local SQLite data, uploads, and SSL certs (all live under /data at runtime)
data/
*.db
*.db-journal
*.db-wal
*.db-shm

# Legacy PHP-app data kept locally for the one-time upgrade import; must never be
# committed or baked into published Docker images (see lib/legacyImport.ts).
include/

.env
.env.local
